# Copy values from Supabase Dashboard → Project Settings → API (do not commit real keys).
# Pass at build/run time with --dart-define (see README).

SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-anon-public-key
